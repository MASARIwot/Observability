receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:14250
processors:
extensions:
  health_check: {}
exporters:
  otlp:
    endpoint: jaeger:14250
    tls:
      insecure: true
  prometheus:
    endpoint: "0.0.0.0:9090"
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
    metrics:
      receivers: [otlp]
      exporters: [prometheus]